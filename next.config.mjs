/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: '/powerof', // Replace 'repo-name' with your GitHub repository name
  assetPrefix: '/repo-name', // Add the same prefix for assets

};
export default nextConfig;
